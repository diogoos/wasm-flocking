import('./pkg')
  .then((m) => {
    // let app = new m.FlockingApp();
    
    // function render() {
    //   app.draw();
    //   window.requestAnimationFrame(render);
    // }

    // render();
  })
  .catch(console.error);